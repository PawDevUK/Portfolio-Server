const

const getVisitors = (req, res) => {
    const method = req.method;
    try {
        if (method === 'GET') {
        }
    } catch (error) {
        console.log(error);
    }
};

exports.getVisitors = getVisitors;
